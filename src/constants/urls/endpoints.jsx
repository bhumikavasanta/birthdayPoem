// TODO: Remove 'services' and merge it with actual URLs.
export const services = {
  AUTH_SERVICE_URI: '/auth-service',
};

export const authService = {
  SWAP_TOKEN: '/auth/swap-token',
  CREATE_USER: '/auth/create-user',
  LOGOUT: '/auth/logout',
};
